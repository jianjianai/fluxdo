import { FlutterEngine } from '@ohos/flutter_ohos';

/**
 * 自动生成的插件注册文件
 * 由 flutter 工具在构建时生成，请勿手动修改
 */
export class GeneratedPluginRegistrant {
  static registerWith(engine: FlutterEngine): void {
    // 插件将由 flutter 构建工具自动注册
  }
}
